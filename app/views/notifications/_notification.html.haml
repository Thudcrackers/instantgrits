%li
  = "#{notification.read == true ? "Read" : "Unread"}"
  = link_to "#{notification.notified_by.user_name} has #{notification.notice_type == "comment" ? "commented on" : "liked"} your post", link_through_path(notification)
  = "#{time_ago_in_words(notification.created_at)} ago"